import React from "react";

function ScrollDownBtn({ onClick }) {
  return (
    <button
      className="bottom-2 left-[50%] translate-x-[-50%] absolute bg-blue-500 p-1 rounded"
      onClick={onClick}
    >
      <svg
        xmlns="http://www.w3.org/2000/svg"
        fill="none"
        viewBox="0 0 24 24"
        strokeWidth={1.5}
        stroke="currentColor"
        className="w-6 h-6 text-white"
      >
        <path
          strokeLinecap="round"
          strokeLinejoin="round"
          d="M19.5 8.25l-7.5 7.5-7.5-7.5"
        />
      </svg>
    </button>
  );
}

export default ScrollDownBtn;
